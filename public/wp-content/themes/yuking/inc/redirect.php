<?php
/**
 * リダイレクト設定
 */

// $cats = get_the_category($post->ID);
// $cat  = $cats[0];

// // 固定ページ「サービス 一覧」の場合はアーカイブ「サービス」へリダイレクト
// if ( is_page('service_top') ) {
//   header( "HTTP/1.1 301 Moved Permanently" );
//   header( "Location: " . esc_url( home_url('/service/') ), true, 301 );
//   exit;
// }
// unset( $cats, $cat );
